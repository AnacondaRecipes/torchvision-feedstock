# macOS 12.3 or above is required for running the GPU variant (MPS support). No way to specify this for only the GPU
# variant, so it's specified for both.
extra_labels_for_os:
  osx-arm64: [ventura]

channels:
  - https://staging.continuum.io/pbp/fs/pytorch-feedstock/pr81/a90ade7
  - https://staging.continuum.io/pbp/fs/triton-feedstock/pr8/194bd41